set verify off

   define pfx   = tq84_

   define low   = lower(
   define wol   = )
-- define low   = ""
-- define wol   = ""

-- define ci    = " collate BINARY_CI"
   define ci    = ""

-- Options
--  - Set pfx to `""` (or change it) to control the name prefix.
--  - Set low/wol to `""` to keep original casing.
--  - Set ci to `" collate binary_ci"` to make comparisons case-insensitive.
